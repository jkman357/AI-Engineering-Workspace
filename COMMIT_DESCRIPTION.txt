fix: centralize native Firefox activation ownership in v0.0.6rc16

* continue the active v0.0.6 RC line without freezing or advancing the minor version
* treat rc15 real-machine failures as one native Browser activation-ownership problem instead of separate Browser-count, Show IDs, maximize, and IME defects
* keep FirefoxInputCoordinator as the single authority for one-shot cross-thread root-HWND focus transactions using temporary AttachThreadInput, SetFocus, and immediate detach in finally
* add central active docked Firefox root-HWND ownership so Workspace transitions recover the Browser the user actually operated instead of guessing a pane
* observe WM_PARENTNOTIFY mouse-down and WM_MOUSEACTIVATE on BrowserDockHost and perform native Firefox focus recovery when the user clicks hosted Firefox content
* stop relying on WPF PreviewMouseDown as the only Browser activation signal because hosted Win32 input can bypass WPF routed mouse events
* propagate native Browser activation to the owning BrowserTile/MainWindow for pane z-order while keeping Firefox internal address-bar/web-content focus transitions under Firefox control
* mark docked Browser roots active from WPF Browser chrome and clear active-Browser ownership when a File pane is activated to prevent layout recovery from stealing File Manager focus
* restore only the active Browser after Show IDs enable/disable, layout-mode toggle, Add Browser, Workspace viewport resize, and explicit Browser maximize/restore transitions
* refit and final-repaint the active Browser before deferred ContextIdle focus recovery after WPF-owned layout changes
* synchronize stale Firefox-thread input-language state by posting WM_INPUTLANGCHANGEREQUEST to the active Firefox root when WPF receives WM_INPUTLANGCHANGE and the Workspace/Firefox HKLs differ
* keep input-language synchronization request-based only; do not call ActivateKeyboardLayout, synthesize WM_IME_COMPOSITION, or proxy IME composition data
* retain Firefox child-HWND focus-guessing removal and do not enumerate/rank compositor/content child HWNDs for normal input
* expand runtime diagnostics with active Browser HWND ownership, native activation reasons, focus transaction results, HKL mismatch state, and input-language request results
* add regression checks for native WM_PARENTNOTIFY/WM_MOUSEACTIVATE activation, central active-root ownership, active-only layout recovery, File-pane focus isolation, and active-root input-language synchronization
* document a real-machine B1-B8 PASS gate covering repeated cross-pane English/number and Zhuyin input, Show IDs on/off, close/reopen cycles, File-to-Browser switching, and maximize/restore without pressing Focus
* stamp application/build/run/test metadata as v0.0.6rc16 / 0.0.6-rc16 / FileVersion 0.0.6.16 from Directory.Build.props
* retain New Workspace confirmation, .aew Workspace persistence, F1/F2 + B1-B4 defaults, Auto Fit, Google startup, 64x64 endpoint badges, native Windows Shell/TortoiseGit/7-Zip integration, asynchronous Git decoration, Firefox launch ownership cleanup, diagnostics/security wording, standard-user design, API-free architecture, and the MIT License unchanged
* preserve source packaging: version in ZIP filename only and extracted root fixed as AI-Engineering-Workspace\
* update README, CHANGELOG, rc16 release notes, regression tests, commit description, and UI/build metadata for v0.0.6rc16
