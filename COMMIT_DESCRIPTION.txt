fix: harden docked Firefox input focus and toolbar UX in v0.0.6rc04

- continue the active v0.0.6 RC line without freezing or advancing the version
- release WPF URL TextBox keyboard focus before transferring input to a docked Firefox HWND
- add a best-effort Win32 browser-content focus bridge for cross-process HwndHost docking
- enumerate visible Firefox child HWNDs and prefer Mozilla compositor/content surfaces as keyboard-focus targets
- return focus toward Firefox web content after Ctrl+L URL navigation so input is not intentionally left in the address bar
- bridge WPF Tab/GotKeyboardFocus entry into BrowserDockHost toward the Firefox content surface
- keep Focus as an explicit recovery and diagnostic action rather than a normal per-input requirement
- add runtime focus diagnostics for root/target HWND, PID/thread, foreground HWND, previous/current focus HWND, and AttachThreadInput state
- replace Browser Launch + Dock, Dock Existing, Focus, and Detach text controls with compact icon buttons and hover tooltips
- retain adaptive layout, Browser maximize/restore, File Manager sorting/actions, diagnostics policy, security wording, standard-user design, and Firefox lifecycle ownership from v0.0.6rc03
- retain the MIT License unchanged
- update README, CHANGELOG, release notes, application metadata, and command-line version labels to v0.0.6rc04
