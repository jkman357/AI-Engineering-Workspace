fix: complete Auto Fit, native HWND repaint, and Git status UI in v0.0.6rc08

- continue the active v0.0.6 RC line without freezing or advancing the version
- make Auto Fit own the visible viewport geometry instead of preserving stale Free Layout canvas dimensions
- make a single remaining File or Browser pane automatically fill the Workspace from the top-left without manual maximize
- keep multi-pane Auto Fit deterministic and reset stale horizontal/vertical scroll offsets after reflow
- suppress intermediate Firefox redraw while committing foreign child-HWND geometry with SetWindowPos
- perform a stronger final repaint across the Firefox HWND, native host HWND, and WPF parent HWND to reduce stale pixels after resize/maximize/reflow
- invalidate and update the native host chain after final geometry changes while retaining Firefox as the browser/navigation authority
- detect Git repository-root folders when browsing a non-repository parent directory such as Desktop or the user profile
- show visible repository summary badges for clean, modified, added/untracked, and deleted working-tree state
- retain read-only local git.exe status probing for files/folders inside repositories
- retain native Windows Shell context menus so registered 7-Zip, compare, TortoiseGit, Open With, Properties, and other Shell extensions remain available
- retain endpoint identity badges, Free Layout move/resize, browser lifecycle ownership, standard-user design, API-free architecture, diagnostics/security wording, and the MIT License unchanged
- update README, CHANGELOG, release notes, application metadata, and command-line version labels to v0.0.6rc08
