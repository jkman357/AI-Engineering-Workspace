fix: restore docked Firefox keyboard input and confirm New Workspace resets in v0.0.6rc12

- continue the active v0.0.6 RC line without freezing or advancing the minor version
- stop normal Browser input handling from enumerating or forcing focus into guessed Firefox compositor/content child HWNDs
- keep a persistent AttachThreadInput bridge between the WPF UI thread and the reparented Firefox root-window thread for the lifetime of each dock
- establish the input bridge immediately after SetParent and release it when the browser is detached, lost, or the host is destroyed
- let Firefox own its internal address-bar and web-content focus transitions after normal mouse clicks
- keep TabIntoCore and the Focus toolbar action as minimal Firefox root-HWND recovery paths only
- retain focus diagnostics for Workspace thread, Firefox thread, foreground HWND, previous/current focus HWND, and bridge state
- make New Workspace use a dedicated confirmation path instead of silently returning true when the current Workspace is clean
- show an explicit Create New / Cancel confirmation before resetting a clean Workspace
- retain Save / Discard / Cancel semantics when New Workspace is invoked with unsaved changes
- preserve deterministic New Workspace reset behavior, default F1/F2 + B1-B4 layout, Auto Fit state, Google Browser startup, and status feedback
- add regression checks for the persistent root-thread input bridge, removal of internal child-HWND focus guessing, explicit New Workspace confirmation, and rc12 version metadata
- stamp application/build/run/test metadata as v0.0.6rc12 / 0.0.6-rc12 / FileVersion 0.0.6.12 from Directory.Build.props
- retain rc11 .aew Workspace persistence, 64x64 endpoint badges, compact version display, native Windows Shell/TortoiseGit/7-Zip integration, asynchronous Git decoration, Firefox launch ownership cleanup, diagnostics/security wording, standard-user design, API-free architecture, and the MIT License unchanged
- update README, CHANGELOG, rc12 release notes, tests, and UI/build metadata for v0.0.6rc12
