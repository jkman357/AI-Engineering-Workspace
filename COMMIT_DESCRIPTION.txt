fix: harden rc08 review findings in v0.0.6rc09

- continue the active v0.0.6 RC line without freezing or advancing the version
- replace Auto Fit fixed grid sizing with a minimum-size-aware layout planner so pane placement stride and actual pane dimensions cannot overlap
- enlarge the Auto Fit Canvas and allow scrolling when supported pane counts physically cannot fit the current viewport
- keep the single-pane Auto Fit invariant that one remaining pane fills the Workspace
- move Git status/repository probing off the WPF dispatcher so file/folder entries render before Git decoration completes
- add cancellable Git probes, navigation-generation guards, short snapshot caching, and cancellation-aware git.exe process waits
- prevent stale Git results from a previous folder navigation from updating the current File pane
- fix porcelain v1 -z rename/copy parsing to keep the current/destination path and only consume the following old/source path
- collapse the Git overlay when GitGlyph is empty so non-Git items do not show blank badge boxes
- treat Firefox launch as a pending transaction inside FirefoxWindowService immediately after Process.Start
- add synchronous Workspace-shutdown cleanup for a pending Firefox launch before HWND discovery/BrowserTile ownership recording completes
- clean ambiguous, canceled, and timeout launch transaction candidates while preserving the pre-launch Firefox HWND baseline
- centralize executable version authority in Directory.Build.props and derive application/build/run/test version labels from assembly/build metadata
- stamp rc09 as Version 0.0.6-rc09, FileVersion 0.0.6.9, and InformationalVersion v0.0.6rc09
- add a dependency-free AIEngineeringWorkspace.Tests regression project and root test.cmd
- add regression checks for 12-pane Auto Fit non-overlap, single-pane fill, Git rename destination parsing, empty Git-badge collapse markup, and rc09 version consistency
- update README, CHANGELOG, rc09 release notes, solution/project structure, scripts, and diagnostics folders
- retain native Windows Shell context menus, TortoiseGit/7-Zip/compare Shell integration, Firefox HWND docking, security/privacy wording, standard-user design, API-free architecture, and the MIT License unchanged
