refactor: prototype native top-level Firefox pseudo-docking in v0.0.6rc19

* continue the active v0.0.6 RC line without freezing or advancing the minor version
* use rc18 real-machine evidence that English/number input can work with native pass-through while Zhuyin/Chinese IME composition still fails under cross-process SetParent embedding
* stop extending the reparented Firefox focus/IME workaround chain and change the Browser hosting boundary instead
* replace Firefox SetParent embedding with a native top-level pseudo-dock model in which BrowserDockHost is only a WPF-owned geometry anchor
* explicitly keep Firefox out of WS_CHILD mode and preserve it as a native top-level HWND while removing visible caption/frame chrome during pseudo-docking
* assign the Workspace top-level HWND only as the Firefox window owner through GWL_HWNDPARENT, not as its parent, and restore the original owner on detach
* mirror each Browser pane to screen coordinates using the anchor GetWindowRect plus SetWindowPos with SWP_NOACTIVATE and no input-queue manipulation
* synchronize pseudo-docked Firefox geometry after pane/layout resize, Workspace viewport resize, scrolling, MainWindow movement, Window activation, and restore transitions
* hide pseudo-docked Firefox windows when their pane or the Workspace is hidden/minimized and resynchronize them when visible again
* remove AttachThreadInput and root SetFocus from the rc19 Firefox architecture entirely
* change the Focus toolbar action, TabIntoCore, and Workspace-driven navigation recovery to native top-level activation through SetForegroundWindow
* infer active Browser ownership from the native foreground Firefox top-level during health diagnostics instead of intercepting child WM_PARENTNOTIFY messages
* retain read-only HKL, GetGUIThreadInfo, input-language, and IME diagnostics while keeping input-language synchronization, ActivateKeyboardLayout, synthetic IME composition, and Firefox child-HWND focus guessing disabled
* retain non-focusable Workspace and Browser chrome introduced in rc18
* document rc19 as a prototype with explicit validation requirements for Free Layout clipping/scrolling, overlapping panes, multi-monitor/mixed-DPI movement, minimize/restore, Alt-Tab, and isolation from external Firefox windows
* add regression checks for no Firefox SetParent call, top-level WS_POPUP/owner semantics, screen-rectangle synchronization, no AttachThreadInput/SetFocus, SetForegroundWindow recovery, diagnostic-only IME handling, and the staged manual gate
* stage the real-machine PASS gate from B1 English/number + Zhuyin to B1-B4 and only then B1-B8 before treating the pseudo-dock architecture as viable
* stamp application/build/run/test metadata as v0.0.6rc19 / 0.0.6-rc19 / FileVersion 0.0.6.19 from Directory.Build.props
* retain New Workspace confirmation, .aew Workspace persistence, F1/F2 + B1-B4 defaults, Auto Fit, Google startup, 64x64 endpoint badges, native Windows Shell/TortoiseGit/7-Zip integration, asynchronous Git decoration, Firefox launch ownership cleanup, diagnostics/security wording, standard-user design, API-free architecture, and the MIT License unchanged
* preserve source packaging: version in ZIP filename only and extracted root fixed as AI-Engineering-Workspace\
* update README, CHANGELOG, rc19 release notes, regression tests, commit description, and UI/build metadata for v0.0.6rc19
