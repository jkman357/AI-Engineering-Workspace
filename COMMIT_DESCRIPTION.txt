fix: restore Firefox input after Browser maximize and restore in v0.0.6rc15

* continue the active v0.0.6 RC line without freezing or advancing the minor version
* retain rc14 multi-Browser English/number and Zhuyin input behavior and its read-only IME/input-language diagnostics
* treat Browser Workspace maximize/restore as a separate WPF-owned focus transition rather than changing the general Firefox keyboard or IME architecture
* defer Firefox root-HWND focus recovery until maximize visibility/geometry changes have completed
* refit and final-repaint the hosted Firefox HWND before returning keyboard focus after a maximize/restore transition
* clear WPF keyboard focus from the maximize/restore button before handing focus back to Firefox
* route maximize/restore recovery through the existing centralized FirefoxInputCoordinator transactional AttachThreadInput + SetFocus path
* add reasoned BrowserDockHost root-focus recovery so runtime diagnostics identify MaximizeCompleted and RestoreCompleted transitions
* request focus restoration only for explicit Browser maximize/restore actions, avoiding unintended Browser focus theft when maximize state is cleared as a side effect of Add, layout-mode, or save operations
* keep the Focus toolbar action as recovery/diagnostic only and do not require it for normal, maximized, or restored Browser typing
* keep Firefox child-HWND guessing removed and do not synthesize WM_IME_COMPOSITION, activate keyboard layouts, or force Firefox internal IME state
* add regression checks for deferred ContextIdle focus recovery, WPF focus release, maximize/restore reason diagnostics, and centralized root-HWND handoff
* document a real-machine PASS gate for B1/B3 normal, maximize, and restore English/number + Zhuyin input plus cross-pane maximize/restore switching
* stamp application/build/run/test metadata as v0.0.6rc15 / 0.0.6-rc15 / FileVersion 0.0.6.15 from Directory.Build.props
* retain New Workspace confirmation, .aew persistence, F1/F2 + B1-B4 defaults, Auto Fit, Google startup, endpoint badges, native Windows Shell/TortoiseGit/7-Zip integration, asynchronous Git decoration, Firefox launch ownership cleanup, security/privacy wording, standard-user design, API-free architecture, and the MIT License unchanged
* preserve source packaging: version in ZIP filename only and extracted root fixed as AI-Engineering-Workspace\
* update README, CHANGELOG, rc15 release notes, regression tests, commit description, and UI/build metadata for v0.0.6rc15
