fix: roll Firefox input handling back to the pre-rc16 baseline in v0.0.6rc22

* continue the active v0.0.6 RC line without freezing or advancing the minor version
* use the rc14-rc21 real-machine investigation history to return the active Browser input architecture to the rc15-era baseline before corrective Firefox input-language synchronization was introduced
* restore the rc15 SetParent Browser docking path and centralized one-shot Firefox root-HWND recovery model instead of carrying forward the rc16 native-click coordinator, rc17 persistent bridge, or rc18-rc21 pass-through/pseudo-dock/launch-only experiments
* keep FirefoxInputCoordinator as the only cross-thread root-focus recovery authority using temporary AttachThreadInput, SetFocus on the Firefox root HWND, and immediate detach in finally
* retain no dock-lifetime or per-Firefox-thread persistent AttachThreadInput bridge
* keep normal Browser input free of Firefox compositor/content child-HWND enumeration, ranking, or focus guessing
* remove the rc16+ corrective input-language design from the active path and never post WM_INPUTLANGCHANGEREQUEST into Firefox
* keep Workspace/Firefox HKL, foreground HWND, GetGUIThreadInfo active/focus/caret HWND, and IME/input-language messages as read-only diagnostics only
* do not call ActivateKeyboardLayout for Firefox and do not synthesize WM_IME_COMPOSITION or other IME composition data
* do not install WM_PARENTNOTIFY / WM_MOUSEACTIVATE driven automatic root-focus handoff on every native Firefox click
* retain rc15 deferred maximize/restore focus recovery after WPF layout/render completes without changing Firefox input-language state
* preserve rc16-rc21 release notes as investigation history while making clear that those experimental Browser input/hosting architectures are not active in rc22
* require a fresh Firefox process for the first rc22 Zhuyin validation to avoid confusing persistent Firefox process state from earlier experimental RCs with the rc22 code path
* add regression checks that input-language handling remains diagnostic-only, no corrective PostMessage path exists, no native-click focus coordinator exists, no persistent input bridge exists, and the rc22 fresh-process validation gate is documented
* split README and CHANGELOG responsibilities: README contains current product/architecture/build/security information only, CHANGELOG contains version history, and docs/releases contains detailed per-RC engineering notes
* stamp application/build/run/test metadata as v0.0.6rc22 / 0.0.6-rc22 / FileVersion 0.0.6.22 from Directory.Build.props
* retain New Workspace confirmation, .aew Workspace persistence, F1/F2 + B1-B4 defaults, Auto Fit, Google startup, 64x64 endpoint badges, native Windows Shell/TortoiseGit/7-Zip integration, asynchronous Git decoration, Firefox launch ownership cleanup, diagnostics/security wording, standard-user design, API-free architecture, and the MIT License unchanged
* preserve source packaging: version in ZIP filename only and extracted root fixed as AI-Engineering-Workspace\
