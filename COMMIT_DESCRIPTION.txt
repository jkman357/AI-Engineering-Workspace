diagnostic: trace Firefox IME and input-language state in v0.0.6rc14

* continue the active v0.0.6 RC line without freezing or advancing the minor version
* retain rc13 centralized transactional Firefox root-HWND focus handoff for ordinary keyboard input
* treat real-machine English/number PASS versus Zhuyin/IME FAIL as a separate input-language/composition investigation path
* add GetKeyboardLayout diagnostics for both the WPF Workspace UI thread and the docked Firefox window thread
* add GetGUIThreadInfo diagnostics for Firefox active HWND, focus HWND, caret HWND, foreground HWND, PID, and input thread
* sample each docked Browser from the existing one-second health loop and emit diagnostics only when observable input state changes
* record WM_INPUTLANGCHANGEREQUEST and WM_INPUTLANGCHANGE when they reach the WPF MainWindow or WPF-owned Browser host HWND
* record WM_IME_SETCONTEXT, WM_IME_STARTCOMPOSITION, WM_IME_COMPOSITION, and WM_IME_ENDCOMPOSITION on the observable WPF-side HWND boundaries
* expand Firefox focus-handoff diagnostics with Workspace/Firefox HKL values before and after SetFocus plus GUI-thread focus evidence
* deliberately do not synthesize IME composition messages, call ActivateKeyboardLayout, or force a keyboard-layout change into Firefox before evidence identifies the failing boundary
* add regression checks for HKL/GUI-thread P/Invokes, input-language/IME message instrumentation, periodic state-transition diagnostics, and the read-only rc14 investigation policy
* document a Windows manual PASS/FAIL gate covering English/number input, English-to-Zhuyin switching, Zhuyin composition, return to English/number input, and runtime-log capture
* stamp application/build/run/test metadata as v0.0.6rc14 / 0.0.6-rc14 / FileVersion 0.0.6.14 from Directory.Build.props
* retain rc12 explicit New Workspace confirmation and Save / Discard / Cancel behavior for unsaved changes
* retain .aew Workspace persistence, F1/F2 + B1-B4 defaults, Auto Fit, Google Browser startup, endpoint badges, native Windows Shell/TortoiseGit/7-Zip integration, asynchronous Git decoration, Firefox launch ownership cleanup, diagnostics/security wording, standard-user design, API-free architecture, and the MIT License unchanged
* preserve the source packaging convention: version only in the ZIP filename and extracted root fixed as AI-Engineering-Workspace\
* update README, CHANGELOG, rc14 release notes, regression tests, commit description, and UI/build metadata for v0.0.6rc14
